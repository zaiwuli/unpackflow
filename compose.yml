services:
  unpackflow:
    image: ghcr.io/zaiwuli/unpackflow:latest
    container_name: unpackflow
    restart: unless-stopped
    environment:
      TZ: ${TZ:-Asia/Shanghai}
    volumes:
      - /volume2/解压目录:/data
      - /volume1/CloudNAS/CloudDrive:/volume1/CloudNAS/CloudDrive
      - /volume1/docker/unpackflow:/config
    ports:
      - 8066:5656
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:5656/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
